# `<nombre comercial del servicio>`

> **Molde.** Copia esta carpeta a `packs/<id-del-servicio>/` y completa lo que está entre `< >`.
> Esta carpeta **no se borra**: es el molde para el próximo servicio que agregues.

**Objetivo.** El objetivo de este servicio es `<qué problema del cliente resuelve, en una frase que
el cliente reconocería como su problema>`.

**A quién se le vende.** `<tipo de cliente y en qué situación lo necesita>`

## Señales que activan este pack

Las palabras y situaciones con que un cliente pide esto, **en su lenguaje y no en el tuyo**. Estas
mismas señales van a `registry.yaml`.

- `<frase típica del cliente>`
- `<situación que lo dispara>`

## Qué incluye

- `<actividad o entregable incluido>`
- `<actividad o entregable incluido>`

## Qué NO incluye

Lo más importante del documento: es lo que evita la discusión a mitad del trabajo.

- `<lo que la gente asume incluido y no está>` → `<se cotiza aparte / lo hace otro / no lo hacemos>`
- `<lo que se confunde con este servicio>`

## Entradas mínimas exigibles al cliente

Sin esto el trabajo no puede empezar. Si falta, se pide antes de comprometer plazo.

| Entrada | Por qué es indispensable | Si no está |
|---|---|---|
| `<documento, dato, acceso o medición>` | `<qué no se puede hacer sin eso>` | `<se levanta y se cobra / se detiene / se asume con supuesto declarado>` |

## Especialistas que participan

| Especialista | En qué parte interviene |
|---|---|
| `<id de staff/>` | `<qué resuelve dentro de este servicio>` |

## Entregables

| Entregable | Formato | Guion que sigue |
|---|---|---|
| `<qué recibe el cliente>` | `<informe, presentación, planilla, plan>` | `<plantillas/estructura-entregable.md u otro>` |

## Cómo se cotiza

`<la unidad comercial de este servicio: por cantidades ejecutadas, por fase, por jornada, por
retainer>`. Los valores no van acá: viven en el `costos.md` de cada especialista.

## Plazo típico

`<duración normal>` · **Depende de:** `<qué lo alarga o lo acorta>`

## Supuestos comerciales y riesgos

| Supuesto o riesgo | Efecto si no se cumple |
|---|---|
| `<lo que se asume al cotizar>` | `<qué pasa con el precio, el plazo o el alcance>` |

## Límites y advertencias

- `<lo que este servicio nunca promete como resultado>`
- `<lo que requiere firma o certificación de un profesional habilitado>`
