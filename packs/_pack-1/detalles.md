# `<nombre comercial del servicio>` — ejemplo de servicio de EJECUCIÓN

> **ACÁ VA UN EJEMPLO.** Este pack no es real: muestra la forma de un servicio que **vende trabajo
> hecho** —obra, producción, implementación, fabricación—, donde el cliente compra un resultado
> material y medible.
> **Bórralo y deja lo que tú necesitas ofrecer.** El molde limpio está en
> `packs/_PLANTILLA/detalles.md`.

**Objetivo.** El objetivo de este servicio es `<ejecutar correctamente un trabajo ya definido y
entregarlo verificado>`.

**A quién se le vende.** `<cliente que ya sabe qué necesita y busca quién lo haga bien>`

## Señales que activan este pack

- `<"necesito que me hagan...">`
- `<"se nos rompió / se nos desgastó / hay que reponer...">`
- `<situación operativa concreta que exige intervenir>`

## Qué incluye

- `<la ejecución del trabajo, según la especificación recibida>`
- `<la cuantificación previa: cuánto hay que hacer y en qué unidad>`
- `<los controles de calidad e inspección propios del oficio>`
- `<la entrega verificada: qué se revisa antes de decir que está listo>`

## Qué NO incluye

- **El diseño o la definición técnica.** Este servicio ejecuta una decisión ya tomada → `<si el
  cliente no la tiene, se vende primero el servicio de diagnóstico>`
- `<permisos, autorizaciones o trámites>` → `<los gestiona el cliente>`
- `<lo que debe poner el cliente: energía, accesos, andamios, espacio, insumos>`
- `<trabajos de otras especialidades que aparezcan durante la ejecución>` → se reportan y se
  cotizan aparte

## Entradas mínimas exigibles al cliente

| Entrada | Por qué es indispensable | Si no está |
|---|---|---|
| `<la especificación o definición técnica aprobada>` | Sin ella no hay qué ejecutar, solo interpretación | `<se detiene, o se vende el diagnóstico primero>` |
| `<las medidas o cantidades verificables, o acceso para medirlas>` | Todo el precio depende de la cantidad | `<se levanta en visita y se cobra como partida>` |
| `<la ventana real de trabajo: turnos, accesos, coordinación>` | Cambia el rendimiento y el costo | `<se cotiza con el escenario más restrictivo, declarado>` |

## Especialistas que participan

| Especialista | En qué parte interviene |
|---|---|
| `<id del especialista de ejecución>` | `<el trabajo propiamente tal y su cuantificación>` |
| `<id del especialista transversal>` | `<revisión final: coherencia, procedencia de cifras y forma>` |

## Entregables

| Entregable | Formato | Guion que sigue |
|---|---|---|
| `<el trabajo ejecutado>` | `<físico o digital>` | — |
| `<informe de ejecución con cantidades finales y controles aplicados>` | `<documento>` | `plantillas/estructura-entregable.md` |

## Cómo se cotiza

**Por cantidades ejecutadas.** `<unidad del oficio>` por valor unitario, más factores por condición
de trabajo. Los valores viven en el `costos.md` del especialista, no acá.

## Plazo típico

`<duración normal>` · **Depende de:** `<disponibilidad de acceso, turnos, provisión de materiales,
condiciones del lugar>`

## Supuestos comerciales y riesgos

| Supuesto o riesgo | Efecto si no se cumple |
|---|---|
| `<las cantidades declaradas por el cliente son correctas>` | `<el precio se ajusta contra medición real>` |
| `<el acceso está disponible en la ventana acordada>` | `<esperas cobrables y corrimiento de plazo>` |
| `<no hay interferencias no declaradas>` | `<se reporta, se detiene la partida afectada y se recotiza>` |

## Límites y advertencias

- No se garantiza `<resultado que depende de condiciones fuera del control de la ejecución>`.
- `<lo que requiere firma o certificación de un profesional habilitado>` no se entrega resuelto: se
  marca como pendiente de firma.
