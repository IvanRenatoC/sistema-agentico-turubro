# `<nombre del documento o norma>` — extracto operativo

> **ACÁ VA UN EJEMPLO.** Este archivo muestra la forma de un extracto operativo.
> **Bórralo y deja los tuyos**, uno por documento que uses seguido, con el nombre del documento:
> `refs/<nombre-del-documento>.md`.
>
> **Escrito con palabras propias.** No se transcribe el texto de la norma, ni sus tablas completas,
> ni sus figuras: se describe cómo se aplica y se cita la fuente. Ver `refs/README.md`.

**Objetivo del extracto.** El objetivo de este documento es `<dejar utilizable, en el día a día, la
parte de este estándar que efectivamente se aplica en nuestro trabajo>`.

**Alcance.** Se aplica a `<qué tipo de trabajos, materiales, situaciones o entregables>`.

## Qué exige, en términos prácticos

| Exigencia | Cómo se aplica en nuestro trabajo | Sección de referencia |
|---|---|---|
| `<qué obliga, dicho con tus palabras>` | `<qué hacemos concretamente para cumplirlo>` | `<sección o cláusula del documento>` |
| `<otra exigencia>` | `<cómo se cumple>` | `<sección>` |

## Qué decide en una corrida

Las preguntas que este extracto resuelve. Si una no aparece acá, hay que ir al documento original.

- `<pregunta típica que se responde con esto>` → `<la respuesta operativa>`
- `<otra pregunta>` → `<respuesta>`

## Qué NO cubre este extracto

- `<materia que la norma trata y aquí no se resume, porque no aplica a nuestro trabajo>`
- `<casos especiales que exigen leer el documento completo>`
- `<lo que requiere interpretación de un profesional habilitado>`

## Cómo se cita en un entregable

Formato de cita que usa la oficina:

```
<Nombre del documento> <versión o año>, <sección> — <qué exige, en una línea>
```

`<Ajusta el formato al que exija tu rubro o tu cliente.>`

## Fuente y vigencia

| Campo | Valor |
|---|---|
| Documento | `<nombre completo>` |
| Emisor | `<organismo o entidad>` |
| Versión / año | `<versión vigente>` |
| Original | `<originales/<archivo> o dónde conseguirlo>` |
| Fecha de este extracto | `<fecha>` |
| Próxima revisión | `<fecha>` |

## Historial de revisión

| Fecha | Qué cambió en el extracto | Por qué |
|---|---|---|
| `<fecha>` | `<primera versión / qué se actualizó>` | `<nueva versión de la norma / hallazgo en una corrida>` |
