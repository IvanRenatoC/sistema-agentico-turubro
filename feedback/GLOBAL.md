# Aprendizajes globales

> **Este archivo no se borra: se reemplaza su contenido.** Lo que va acá aplica a **toda** la
> oficina, sin importar el especialista ni el servicio. Si la corrección es de una pieza concreta,
> va a `feedback/staff/<id>.md` o `feedback/packs/<id>.md`.
>
> Lo lee el coordinador al inicio de cada corrida, junto con `CLAUDE.md`.

## Reglas nuevas de la casa

Correcciones ya promovidas a `CLAUDE.md`, anotadas acá para que se sepa de dónde salieron.

| Fecha | Regla | Origen | Dónde quedó |
|---|---|---|---|
| `<fecha>` | `<la regla, en una línea imperativa>` | `<corrida o situación que la motivó>` | `<bloque o regla de CLAUDE.md>` |

## Sesgos y errores recurrentes

Lo que se repite en varias corridas y todavía no tiene regla escrita.

| # | Qué ocurre | Veces observado | Qué haríamos distinto | Estado |
|---|---|---|---|---|
| 1 | `<el patrón, concreto>` | `<cuántas>` | `<la corrección>` | `<pendiente / propuesto / incorporado el <fecha>>` |

## Anotaciones abiertas

```markdown
## <fecha> — <corrida donde ocurrió>

**Qué salió mal o faltó.** <descripción concreta, con el dato o la frase exacta>
**Qué debería haber pasado.** <el resultado correcto>
**Por qué ocurrió.** <qué criterio no estaba escrito>
**A qué archivo va la corrección.** <CLAUDE.md, bloque o regla concreta>
**Estado.** <pendiente / propuesto / incorporado el <fecha>>
```

`<Copia el bloque de arriba por cada anotación. Cuando se incorpore, muévela a la tabla de reglas
nuevas y bórrala de acá: este archivo mide lo que falta, no lo que ya se hizo.>`
