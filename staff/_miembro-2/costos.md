# Costos — `<nombre del especialista>`

**Unidad de cobro.** `<la unidad en que se vende el pensamiento en tu rubro: fase, jornada,
entregable, informe, taller>`

> El trabajo de diagnóstico casi nunca se cobra por unidad producida, porque su valor no está en el
> volumen. **Cóbralo por fase o por bloque de tiempo**, y define el alcance de cada fase para que
> no se vuelva infinita.

## Tarifas

| Concepto | Unidad | Valor | Vigencia |
|---|---|---|---|
| `<fase de diagnóstico>` | `<fase / jornada>` | `<valor>` | `<fecha o periodo>` |
| `<fase de definición o decisión>` | `<fase / jornada>` | `<valor>` | `<vigencia>` |
| `<sesión de trabajo con el cliente: entrevista, taller, visita>` | `<sesión>` | `<valor>` | `<vigencia>` |
| `<acompañamiento durante la ejecución>` | `<periodo>` | `<valor>` | `<vigencia>` |

## Rendimientos

| Actividad | Rendimiento | Condiciones en que se cumple |
|---|---|---|
| `<actividad de análisis>` | `<cuánto por jornada>` | `<con qué información disponible>` |
| `<preparación y realización de una sesión>` | `<horas por sesión>` | `<incluye preparación y notas>` |

`<Anota cuánto se demora esto cuando el cliente entrega la información incompleta o a cuentagotas:
es el sobrecosto que más se olvida cotizar.>`

## Factores y recargos

| Situación | Factor |
|---|---|
| `<información dispersa o inexistente, que hay que levantar>` | `<factor>` |
| `<más de N interlocutores o áreas involucradas>` | `<factor>` |
| `<urgencia: plazo menor al normal>` | `<factor>` |
| `<ronda adicional de revisión con el cliente más allá de las incluidas>` | `<factor o valor fijo>` |
| `<situación propia de tu rubro>` | `<factor>` |

## Qué NO incluye el precio

- **La ejecución de lo recomendado:** eso lo cotiza quien lo ejecuta.
- `<estudios, mediciones o investigación con proveedor externo>`
- `<rondas de revisión más allá de <número> incluidas>`
- `<traslados o visitas fuera de <zona>>`

## Fuente y vigencia

`<de dónde salen estos valores: tarifario propio, referencia de mercado, costo de la hora
profesional calculado>`. Se revisan `<cada cuánto>`; última revisión: `<fecha>`.

> **Regla que no se rompe:** si un ítem no tiene tarifa escrita acá, no se inventa. Va como supuesto
> declarado en la partida y se avisa en alertas.
