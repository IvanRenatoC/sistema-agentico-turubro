# Costos — `<nombre del especialista>`

**Unidad de cobro.** `<la unidad en que se mide la producción de este oficio: unidad ejecutada,
metro, pieza, hora hombre, jornada>`

> Elige **una unidad principal** y mantenla. Un oficio que cotiza a veces por unidad y a veces por
> hora termina cobrando distinto por el mismo trabajo.

## Tarifas

| Concepto | Unidad | Valor | Vigencia |
|---|---|---|---|
| `<trabajo estándar, condiciones normales>` | `<unidad>` | `<valor>` | `<fecha o periodo>` |
| `<variante más exigente del mismo trabajo>` | `<unidad>` | `<valor>` | `<vigencia>` |
| `<trabajo menor o de ajuste>` | `<unidad>` | `<valor>` | `<vigencia>` |
| `<hora o jornada, para lo que no cabe en las unidades de arriba>` | `<unidad>` | `<valor>` | `<vigencia>` |

## Rendimientos

| Actividad | Rendimiento | Condiciones en que se cumple |
|---|---|---|
| `<actividad>` | `<cuánto por jornada o por hora>` | `<condiciones normales: acceso, equipo, cuadrilla>` |

`<Anota también el rendimiento cuando las condiciones son malas: es el dato que salva las
cotizaciones de trabajos difíciles.>`

## Factores y recargos

| Situación | Factor |
|---|---|
| `<trabajo en condiciones difíciles: altura, espacio confinado, exterior>` | `<factor>` |
| `<turno nocturno o fin de semana>` | `<factor>` |
| `<operación del cliente en marcha, con interferencias>` | `<factor>` |
| `<urgencia: plazo menor al normal>` | `<factor>` |
| `<traslado o zona alejada>` | `<factor o valor fijo>` |
| `<situación propia de tu oficio>` | `<factor>` |

## Qué NO incluye el precio

- `<lo que pone el cliente: energía, agua, andamios, accesos, permisos>`
- `<lo que se cobra aparte: traslados, esperas, trabajo rehecho por causa ajena>`
- `<lo que hace otra especialidad>`

## Fuente y vigencia

`<de dónde salen estos valores: tarifario propio, cotizaciones de proveedores, costo interno
calculado, referencia de mercado>`. Se revisan `<cada cuánto>`; última revisión: `<fecha>`.

> **Regla que no se rompe:** si un ítem no tiene tarifa escrita acá, no se inventa. Va como supuesto
> declarado en la partida y se avisa en alertas.
