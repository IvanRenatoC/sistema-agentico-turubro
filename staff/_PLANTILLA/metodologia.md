# Metodología — `<nombre del especialista>`

> **Molde.** Completa lo que está entre `< >`. Esta carpeta no se borra.

**Objetivo.** El objetivo de este documento es `<qué debe producir siempre este especialista, y con
qué calidad>`.

## Pasos, en orden

El orden no es decorativo: es donde está el criterio acumulado. No se salta porque el caso parezca
simple.

1. `<primer paso: normalmente verificar qué rige o qué se prometió>`
2. `<segundo paso>`
3. `<tercer paso>`
4. `<cuarto paso>`

## Controles de calidad

- `<qué se verifica antes de entregar>`
- `<qué se verifica antes de entregar>`
- Cada cifra tiene origen declarado: `costos.md`, `refs/`, un adjunto, o supuesto.

## Criterio de aceptación

`<cómo se sabe que el trabajo está bien terminado y no solo terminado. Escríbelo como una prueba
que se pueda aplicar: "alguien de mi oficio podría ejecutar esto sin volver a preguntar", "si un
competidor podría firmar lo mismo, falta decidir">`

## Errores típicos a evitar

- `<error frecuente en tu oficio, y por qué ocurre>`
- `<error frecuente>`
- `<lo que se te cuela cuando el encargo viene apurado>`

## Formato de la respuesta

La partida se escribe en `runs/<corrida>/partidas/<miembro>.md` con el formato que define
`.claude/agents/especialista.md`. Anota acá solo lo propio de este especialista:

- **Secciones adicionales que siempre incluye:** `<las que su oficio exige>`
- **Nivel de detalle esperado:** `<media página o cinco>`
- **Qué nunca omite:** `<lo que se pierde si no lo escribe>`
