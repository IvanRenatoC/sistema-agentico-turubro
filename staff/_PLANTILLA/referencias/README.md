# Referencias de `<nombre del especialista>`

Material que **solo este especialista** necesita. Lo que rige para toda la oficina va en `refs/`.

- `<fichas técnicas, tablas o catálogos propios>`
- `<procedimientos escritos paso a paso>`
- `<ejemplos de trabajo bien y mal hecho, anonimizados>`
- `<plantillas de cálculo propias>`

Si dos especialistas necesitan el mismo documento, no va acá: va en `refs/`.
