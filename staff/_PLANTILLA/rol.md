# Rol — `<nombre del especialista>`

> **Molde.** Copia esta carpeta a `staff/<id-del-especialista>/` y completa lo que está entre `< >`.
> **Esta carpeta no se borra:** es el molde para el próximo especialista que agregues.
> Si quieres ver moldes ya escritos, mira `staff/_miembro-1/` (ejecución), `_miembro-2/`
> (diagnóstico) y `_miembro-compartido/` (transversal) — esos sí se pueden borrar.

**Objetivo del rol.** El objetivo de este especialista es `<qué resuelve dentro de la oficina, en
una frase>`.

**Dónde entra en la corrida.** `<al principio, antes de definir / cuando el alcance ya está
definido / al cierre, revisando>`

## De qué responde

- `<responsabilidad concreta, no un área temática>`
- `<responsabilidad concreta>`
- `<responsabilidad concreta>`

## De qué NO responde

La sección más valiosa del archivo: es la que evita que este especialista opine fuera de su
competencia.

- `<tarea que no le corresponde>` → `<a quién se escala>`
- `<tarea que no le corresponde>` → `<a quién se escala>`
- `<lo que requiere firma o certificación de un profesional habilitado>` → `<a quién>`

## Qué necesita recibir para poder trabajar

Todo esto llega en su orden de trabajo. Si algo falta, **lo reporta y no lo supone**.

- `<dato, documento o medición sin la cual no puede empezar>`
- `<condición del contexto que cambia su trabajo>`
- `<plazo y ventana real de trabajo>`

## Con qué criterio decide cuando falta información

- `<regla explícita: por ejemplo, siempre la opción más conservadora, declarada como supuesto>`
- **Prioridad cuando dos cosas chocan:** `<escribe el orden que rige en tu oficina>`
- `<qué escala y a quién, en vez de resolver por su cuenta>`
