# Costos — `<nombre del especialista>`

> **Molde.** Completa lo que está entre `< >`. Esta carpeta no se borra.

**Unidad de cobro.** `<la unidad en que se mide el trabajo de este especialista: unidad ejecutada,
hora, jornada, fase, entregable>`

> Elige **una unidad principal** y mantenla. Un especialista que cotiza a veces por unidad y a
> veces por tiempo termina cobrando distinto por el mismo trabajo.

## Tarifas

| Concepto | Unidad | Valor | Vigencia |
|---|---|---|---|
| `<trabajo estándar, condiciones normales>` | `<unidad>` | `<valor>` | `<fecha o periodo>` |
| `<variante más exigente>` | `<unidad>` | `<valor>` | `<vigencia>` |
| `<lo que no cabe en las unidades de arriba>` | `<hora o jornada>` | `<valor>` | `<vigencia>` |

## Rendimientos

| Actividad | Rendimiento | Condiciones en que se cumple |
|---|---|---|
| `<actividad>` | `<cuánto por unidad de tiempo>` | `<condiciones normales>` |

`<Anota también el rendimiento cuando las condiciones son malas: es el dato que salva las
cotizaciones de los trabajos difíciles.>`

## Factores y recargos

| Situación | Factor |
|---|---|
| `<condición que encarece el trabajo>` | `<factor o recargo>` |
| `<urgencia: plazo menor al normal>` | `<factor>` |
| `<situación propia de tu oficio>` | `<factor>` |

## Qué NO incluye el precio

- `<lo que pone el cliente>`
- `<lo que se cobra aparte>`
- `<lo que hace otra especialidad>`

## Fuente y vigencia

`<de dónde salen estos valores: tarifario propio, cotizaciones de proveedores, costo interno
calculado, referencia de mercado>`. Se revisan `<cada cuánto>`; última revisión: `<fecha>`.

> **Regla que no se rompe:** si un ítem no tiene tarifa escrita acá, no se inventa. Va como supuesto
> declarado en la partida y se avisa en alertas.
