# Costos — `<nombre del especialista transversal>`

> Un rol transversal se cobra de una de tres maneras. **Elige una y borra las otras dos**, porque
> mezclarlas es la forma más rápida de perder plata sin notarlo:
> `<por corrida (valor fijo) / por unidad de tiempo / absorbido en el gasto general>`.
> Si lo absorbes, escríbelo igual: un costo que no se cobra sigue siendo un costo, y conviene
> saber cuánto es.

**Unidad de cobro.** `<revisión por corrida / hora / jornada / porcentaje del total de la corrida>`

## Tarifas

| Concepto | Unidad | Valor | Vigencia |
|---|---|---|---|
| `<revisión estándar de corrida>` | `<unidad>` | `<valor>` | `<fecha o periodo>` |
| `<revisión de corrida mayor>` | `<unidad>` | `<valor>` | `<fecha o periodo>` |
| `<re-revisión tras correcciones>` | `<unidad>` | `<valor o "sin costo la primera">` | `<vigencia>` |

## Rendimientos

- `<cuántas partidas o páginas se revisan por unidad de tiempo, en condiciones normales>`
- `<qué pasa con el rendimiento cuando el material llega desordenado o incompleto>`

## Factores y recargos

| Situación | Factor |
|---|---|
| `<urgencia: revisión fuera de plazo normal>` | `<factor o recargo>` |
| `<corrida con más de N especialistas>` | `<factor>` |
| `<segunda re-revisión por correcciones no atendidas>` | `<factor>` |
| `<situación propia de tu rubro>` | `<factor>` |

## Qué NO incluye el precio

- La corrección del contenido técnico: eso lo hace y lo cobra el especialista responsable.
- `<lo que se cobra aparte>`
- `<lo que pone el cliente>`

## Fuente y vigencia

`<de dónde salen estos valores: tarifario propio, referencia de mercado, costo interno calculado>`.
Se revisan `<cada cuánto>`, y la última revisión fue el `<fecha>`.

> **Regla que no se rompe:** si un ítem no tiene tarifa escrita acá, no se inventa. Va como supuesto
> declarado en la partida y se avisa en alertas.
