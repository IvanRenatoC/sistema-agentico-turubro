# Brief — `<AAAA-MM-DD-slug-del-encargo>`

> **EJEMPLO ILUSTRATIVO — no es una corrida real.** Muestra la forma del archivo. El formato
> vigente lo define el **coordinador** en `.claude/agents/coordinador.md`; si algo difiere, manda el
> agente. Nadie escribe dentro de `runs/_id-corrida/`: las corridas reales van en
> `runs/AAAA-MM-DD-<slug>/`. Esta carpeta se puede borrar.

**Cliente.** `<nombre>` · **Contacto.** `<persona y cargo>` · **Recibido el.** `<fecha>`

## Objetivo del encargo
`<qué se busca lograr, en una o dos frases, en términos del cliente>`

## Alcance declarado
- `<lo que se entiende incluido>`
- `<lo que se entiende excluido>`

## Plazo
`<fecha comprometida>` · `<de qué depende que se cumpla>`

## Restricciones
| Tipo | Restricción | Efecto en el trabajo |
|---|---|---|
| Operativa | `<turnos, accesos, operación en marcha>` | `<qué cambia>` |
| Normativa | `<qué norma rige y si debe citarse>` | `<qué cambia>` |
| Comercial | `<tope de presupuesto, política de precios>` | `<qué cambia>` |

## Lo afirmado vs. lo demostrado
| Afirmación del cliente | Qué muestra el material | ¿Coinciden? |
|---|---|---|
| `<lo que dijo>` | `<lo que se ve en los adjuntos>` | `<sí / no / no verificable>` |

## Material recibido
| Lote | Archivos | Qué aporta |
|---|---|---|
| `<AAAA-MM-DD-lote>` | `<cantidad y tipo>` | `<para qué sirve>` |

## Pendiente de confirmar
1. `<lo que falta y más cambia el resultado>`
2. `<lo siguiente>`
