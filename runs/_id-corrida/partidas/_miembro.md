# Partida — `<id-del-especialista>` · `<AAAA-MM-DD-slug>`

> **EJEMPLO ILUSTRATIVO — no es una corrida real.** Muestra la forma del archivo. El formato
> vigente lo define el **especialista** en `.claude/agents/especialista.md`; si algo difiere, manda
> el agente. Nadie escribe dentro de `runs/_id-corrida/`: las corridas reales van en
> `runs/AAAA-MM-DD-<slug>/`. Esta carpeta se puede borrar.

## Alcance de esta partida
- **Resolví:** `<qué>`
- **Quedó fuera:** `<qué, y por qué>`

## Desarrollo
`<el trabajo siguiendo los pasos de tu metodología, con las decisiones a la vista y la norma o el
estándar citado cuando corresponda>`

## Cuantificación
| Ítem | Unidad | Cantidad | Valor unitario | Total | Origen |
|---|---|---|---|---|---|
| `<ítem>` | `<unidad>` | `<cantidad>` | `<valor>` | `<total>` | `<costos.md / refs/ / adjunto / supuesto>` |

`<La columna Origen no es opcional: una cifra sin origen es un hallazgo bloqueante en la revisión.>`

## Supuestos
| Supuesto | Qué dato lo resolvería | Efecto si es falso |
|---|---|---|
| `<supuesto>` | `<la medición, dato o confirmación que falta>` | `<qué cambia>` |

## Alertas y riesgos
- `<lo que el coordinador tiene que saber aunque no lo haya preguntado>`

## Fuera de mi competencia
- `<lo que detecté y le corresponde a otro>` → `<a quién>`
